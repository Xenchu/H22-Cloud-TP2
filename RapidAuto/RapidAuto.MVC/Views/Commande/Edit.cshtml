@model RapidAuto.MVC.Models.Commande

@{
    ViewData["Title"] = "Edit";
}

<h1>Modifier</h1>

<h4>Commande</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NumeroIdentifiantUtilisateur" class="control-label"></label>
                <input asp-for="NumeroIdentifiantUtilisateur" class="form-control" />
                <span asp-validation-for="NumeroIdentifiantUtilisateur" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input hidden asp-for="IdVehicule" class="form-control" />
                <span asp-validation-for="IdVehicule" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Enregistrer" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Retourner à la liste des commandes</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
